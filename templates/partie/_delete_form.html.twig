<form method="post" action="{{ path('app_partie_delete', {'idpartie': partie.idpartie}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partie.idpartie) }}">
    <button class="btn">Delete</button>
</form>
