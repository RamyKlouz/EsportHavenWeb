{% extends 'header.oc.html.twig' %}


{% block title %}Modifier Partie{% endblock %}

{% block body %}
    <h1><a href="{{ path('app_partie_index') }}" class="btn btn-primary" type="button" style="border-radius: 50%">←</a> Modifier cet match</h1>


    {{ include('partie/_form.html.twig', {'button_label': 'Update'}) }}

  {#<a href="{{ path('app_partie_index') }}">back to list</a> #}

    {{ include('partie/_delete_form.html.twig') }}
{% endblock %}
