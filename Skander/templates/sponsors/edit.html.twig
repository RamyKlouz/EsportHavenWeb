{% extends 'header.oc.html.twig' %}

{% block title %}Edit Sponsors{% endblock %}

{% block body %}
    <h1><a class="btn btn-primary" href="{{ path('app_sponsors_index') }}" class="btn btn-primary" type="button" style="border-radius: 50%">←</a>Edit Sponsors</h1>

    {{ include('sponsors/_form.html.twig', {'button_label': 'Update'}) }}

    {#  <a href="{{ path('app_sponsors_index') }}">back to list</a>#}

    {{ include('sponsors/_delete_form.html.twig') }}
{% endblock %}
