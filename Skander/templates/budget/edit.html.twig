{% extends 'header.oc.html.twig' %}

{% block title %}Edit Budget{% endblock %}

{% block body %}
    <h1><a class="btn btn-primary" href="{{ path('app_budget_index') }}" class="btn btn-primary" type="button" style="border-radius: 50%">←</a>Edit Budget</h1>

    {{ include('budget/_form.html.twig', {'button_label': 'Mettre a jour'}) }}

    <a class="btn btn-info" href="{{ path('app_budget_index') }}">back to list</a>

    {{ include('budget/_delete_form.html.twig') }}
{% endblock %}
