{% extends 'header.oc.html.twig' %}

{% block title %}Budget{% endblock %}

{% block body %}
    <h1><a class="btn btn-primary" href="{{ path('app_budget_index') }}" class="btn btn-primary" type="button" style="border-radius: 50%">←</a>Budget</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdBudget</th>
                <td>{{ budget.idBudget }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ budget.montant }}</td>
            </tr>
            <tr>
                <th>Activite</th>
                <td>{{ budget.activite }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('app_budget_index') }}">back to list</a>

    <a class="btn btn-primary" href="{{ path('app_budget_edit', {'idBudget': budget.idBudget}) }}">Modifier</a>

    {{ include('budget/_delete_form.html.twig') }}
{% endblock %}
