<form method="post" action="{{ path('app_budget_delete', {'idBudget': budget.idBudget}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ budget.idBudget) }}">
    <button class="btn btn-danger">Delete</button>
</form>
